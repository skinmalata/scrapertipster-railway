name: Daily Ticket Send

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        env:
          RENDER_URL: ${{ secrets.RENDER_URL }}
        run: echo "URL=[$RENDER_URL]"
      - name: Trigger daily ticket send
        env:
          RENDER_URL: ${{ secrets.RENDER_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          curl -v -X POST -H "x-cron-secret: $CRON_SECRET" -H "Content-Type: application/json" "$RENDER_URL/api/newsletter/daily-send"
