services:
  - type: web
    name: winfulltime-api
    runtime: node
    buildCommand: npm ci
    startCommand: npm start
    disk:
      name: live-tip-history
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: LIVE_TIP_HISTORY_FILE
        value: /var/data/live-tip-history.json
